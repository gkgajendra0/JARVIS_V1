# Research-only environment for the Step 4 extraction bake-off.
# Provider SDK/Pydantic versions intentionally match production JARVIS so the
# benchmark measures the same client/schema surface the runtime will execute.
openai==2.54.0
google-genai==2.22.0
pydantic==2.13.5
