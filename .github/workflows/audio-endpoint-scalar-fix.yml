name: Core Audio Scalar Fix

on:
  push:
    branches:
      - feat/governed-jarvis-hands-v1

permissions:
  contents: write

jobs:
  apply:
    if: ${{ !contains(github.event.head_commit.message, '[core-audio-fix]') }}
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/governed-jarvis-hands-v1
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Apply Core Audio fix
        run: python tools/audio_endpoint_scalar_fix.py
      - name: Format and lint touched files
        run: |
          python -m pip install ruff==0.16.5
          ruff format src/jarvis/capabilities/windows_native.py tests/test_windows_native_capabilities.py
          ruff check src/jarvis/capabilities/windows_native.py tests/test_windows_native_capabilities.py
      - name: Remove temporary fix files
        run: |
          rm -f tools/audio_endpoint_scalar_fix.py
          rm -f .github/workflows/audio-endpoint-scalar-fix.yml
      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Fix Core Audio endpoint scalar adapter [core-audio-fix]"
          git push origin HEAD:feat/governed-jarvis-hands-v1
